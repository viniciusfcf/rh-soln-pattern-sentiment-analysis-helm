apiVersion: v1
kind: Service
metadata:
  name: {{ include "products-review.name" . }}
  labels:
    {{- include "products-review.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.containerPort }}
      protocol: TCP
      name: {{ .Values.service.portName }}
  selector:
    {{- include "products-review.selectorLabels" . | nindent 4 }}
